TCP_LISTEN_START=8100
TCP_LISTEN_END=8200
TCP_TARGET_HOST=127.0.0.1
TCP_TARGET_PORT_OFFSET=0
TCP_LOG_COALESCE_MS=10

# HTTP JSON ingress (POST JSON). Target port = HTTP_LISTEN_PORT + HTTP_TARGET_PORT_OFFSET
HTTP_LISTEN_PORT=8100
HTTP_TARGET_HOST=127.0.0.1
HTTP_TARGET_PORT_OFFSET=0

# Telegram Bot API (do not commit real tokens)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
